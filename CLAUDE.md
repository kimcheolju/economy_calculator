# CLAUDE.md — 경제적 자유 계산기 개발 지침

이 문서는 이 저장소에서 작업하는 모든 세션이 **먼저 읽고 따라야 하는 규칙**입니다.
제품 요구사항은 `plan/design-review-v2.md`, 상세 설계는 `design/` 에 있습니다.

---

## 0. 프로젝트 한 줄 요약

대한민국 거주자를 위한 **ETF 장기투자 + 은퇴(경제적 자유) 시뮬레이터**. 완전 클라이언트 사이드 웹앱.
사용자에게 답해야 하는 단 하나의 질문: **"지금 계획대로 투자하면 은퇴 후 오늘 돈 가치로 매달 얼마를 쓸 수 있는가?"**

---

## 1. 절대 규칙 (Non-negotiable)

이 9가지를 위반하는 코드는 머지하지 않습니다.

### R-1. 명목/실질을 절대 섞지 않는다
모든 금액 값은 명목(nominal)인지 실질(real)인지가 **타입 또는 필드명으로 구분**되어야 합니다.
```ts
// ✅ 좋음
interface Money { nominal: number; real: number }   // real = 오늘 구매력 기준
// ❌ 금지
const finalAsset = 1_000_000_000   // 명목인지 실질인지 알 수 없음
```
실질금액의 기준 시점은 **항상 "계산 실행일(오늘)"** 입니다. 다른 기준 시점을 쓰려면 필드명에 명시하세요(`realAtRetirement`).

### R-2. 세율·제도 수치를 코드에 하드코딩하지 않는다
모든 세율·한도·공제액은 `src/data/tax/` 하위의 **버전드 데이터 파일**에만 존재합니다.
계산 함수는 이 값을 **인자로 받습니다**. `0.154`, `2_000_000`, `900_0000` 같은 리터럴이 `src/calc/` 안에 나타나면 잘못된 코드입니다.
```ts
// ✅ 좋음
function taxOnDividend(amount: number, rule: DividendTaxRule): number
// ❌ 금지
function taxOnDividend(amount: number) { return amount * 0.154 }
```

### R-3. 모든 제도 수치에는 출처와 기준일이 붙는다
데이터 파일의 각 항목은 `source`(URL), `asOf`(YYYY-MM-DD), `status`(`"confirmed" | "proposed" | "needs-verification"`)를 **필수로** 가집니다. `status: "proposed"` 항목은 기본 계산에서 제외되고, 사용자가 토글로만 활성화할 수 있습니다.

### R-4. 계산 엔진은 순수 함수다
`src/calc/` 의 모든 함수는 순수 함수입니다. `Date.now()`, `Math.random()`, 전역 상태, I/O, React 훅을 사용하지 않습니다.
- 현재 날짜가 필요하면 **인자로 받습니다**.
- 난수가 필요하면 **시드 기반 PRNG를 인자로 받습니다**(Monte Carlo 재현성 필수).

### R-5. 중간 계산에서 반올림하지 않는다
`number`(float64)로 끝까지 계산하고, **표시 직전에만** 반올림합니다. 월별 루프 안에서 `Math.round()`를 호출하면 30년 × 12개월 누적 오차가 발생합니다.
표시용 반올림은 `src/lib/format.ts` 의 포맷터만 담당합니다.

### R-6. 금융 계산 규약을 임의로 바꾸지 않는다
아래는 확정된 규약입니다. 변경하려면 `design/02-calculation-engine.md` 를 먼저 수정하세요.

| 항목 | 규약 |
|---|---|
| 연 → 월 수익률 | `(1 + annual) ** (1/12) - 1` — **`annual/12` 금지** |
| 실질 수익률 | `(1 + nominal) / (1 + inflation) - 1` — **`nominal - inflation` 금지** |
| ETF 총보수(TER) | 월 배율에 `(1 - ter) ** (1/12)` 를 곱한다 (자산 비례 차감) |
| 납입 시점 | **월초**(annuity-due) 기본. `contributionTiming: 'begin' \| 'end'` 로 전환 |
| 납입금 증액 | 투자 개시 후 12개월마다 1회 |
| 세금 | 발생 시점 과세(배당 원천징수) / 실현 시점 과세(매매차익)를 구분 |

### R-7. 사용자 데이터는 절대 네트워크로 나가지 않는다
소득·자산 정보를 다룹니다. 서버 전송, 외부 API 호출, 애널리틱스로의 입력값 전송을 금지합니다.
상태 저장은 `localStorage` 와 **URL 쿼리 인코딩**만 사용합니다. 외부 폰트·CDN도 쓰지 않고 self-host 합니다.

### R-8. 계산 결과에는 항상 근거가 따라간다
계산 결과 객체는 사용된 가정을 담은 `assumptions` 필드를 함께 반환합니다. UI는 이를 "가정 패널"로 노출합니다. **숫자만 보여주고 근거를 숨기는 화면을 만들지 않습니다.**

### R-9. 확신 없는 세법을 단정하지 않는다
세법 해석이 불확실하면 **추측해서 구현하지 말고**, 데이터 파일에 `status: "needs-verification"` 과 주석으로 남기고 UI에 "확인 필요" 배지를 띄웁니다. 사용자에게 틀린 세금 계산을 확신 있게 보여주는 것이 최악의 실패입니다.

---

## 2. 기술 스택 (확정)

| 영역 | 선택 | 이유 |
|---|---|---|
| 빌드 | **Vite 6** | 정적 배포, 빠른 HMR |
| 언어 | **TypeScript** (`strict: true`) | 금융 계산에서 타입 안전성 필수 |
| UI | **React 19** | |
| 스타일 | **Tailwind CSS v4** | |
| 차트 | **Recharts** | 선언적, 접근성 대응 용이 |
| 상태 | **Zustand** | 단일 입력 스토어 + 파생 계산 |
| 검증 | **Zod** | 입력 스키마 + URL 복원 시 검증 |
| 테스트 | **Vitest** | 계산 엔진 단위 테스트 |
| 무거운 계산 | **Web Worker** (Comlink 없이 순수 postMessage) | Monte Carlo |
| 배포 | 정적 호스팅 (Vercel / GitHub Pages) | 백엔드 없음 |

**추가 의존성은 신중히.** 날짜 라이브러리(연/월 정수 산술로 충분), 상태 머신, i18n 프레임워크(한국어 단일)는 불필요합니다. 새 의존성을 추가하려면 이유를 커밋 메시지에 남기세요.

---

## 3. 디렉터리 구조

```
economy_calculator/
├── CLAUDE.md                 ← 이 문서
├── plan/                     ← 요구사항 (원본 + 검토판)
├── design/                   ← 상세 설계 문서 (구현 전 필독)
├── src/
│   ├── calc/                 ← 🔒 순수 계산 엔진 (React 의존 0)
│   │   ├── accumulate.ts     ← 축적기 월별 시뮬레이션
│   │   ├── withdraw.ts       ← 인출기 시뮬레이션 (전략 4종)
│   │   ├── fire.ts           ← 경제적 자유 필요자산
│   │   ├── solve.ts          ← 역산 솔버 (이분법)
│   │   ├── montecarlo.ts     ← MC + SORR
│   │   ├── tax/              ← 계좌별 과세 계산기
│   │   ├── rates.ts          ← 수익률/물가 변환 유틸
│   │   └── types.ts          ← 입력/출력 타입
│   ├── data/
│   │   └── tax/              ← 🔒 버전드 제도 데이터 (출처·기준일 필수)
│   │       ├── kr-2026.ts
│   │       └── index.ts      ← 기준일로 룰셋 조회
│   ├── store/                ← Zustand 스토어
│   ├── index.css             ← 🔒 디자인 토큰 단일 정의 지점 (design/08)
│   ├── components/           ← UI 컴포넌트
│   │   ├── display/          ← Primitives · Table · Icon (시각 언어)
│   │   ├── inputs/           ← Field · MoneyInput · RateInput · Controls
│   │   └── charts/           ← ChartChrome + 차트들
│   ├── features/             ← 화면 단위 조립
│   ├── lib/                  ← format.ts, url-codec.ts, prng.ts
│   └── workers/              ← 워커 + spawn.ts / spawn.inline.ts
└── tests/
    ├── golden/               ← 손계산·엑셀 대조 케이스 (변경 금지)
    └── ...
```

**`src/calc/` 와 `src/data/` 는 UI를 전혀 모릅니다.** 이 두 폴더에서 `react`, `zustand`, DOM API를 import하면 잘못된 코드입니다.

---

## 4. 코딩 규약

### 금액과 단위
- 내부 표현은 **원(KRW) 단위 `number`** 하나로 통일. 만원·억원 단위 혼용 금지.
- 비율은 **소수**(`0.07` = 7%). 퍼센트 정수(`7`)를 내부에 두지 않음. UI 경계에서만 변환.
- 표시 포맷은 `formatKRW()` 사용 — 한국식(예: `12억 3,456만원`). 직접 `toLocaleString()` 호출 금지.

### 네이밍
- 명목/실질: `nominalX` / `realX`
- 세전/세후: `grossX` / `netX`
- 연/월: `annualX` / `monthlyX`
- 연령 기준 값: `atAge65` 처럼 명시
- 축약어 금지: `retAge` ❌ → `retirementAge` ✅

### 타입
- `any` 금지. 외부 입력은 Zod로 파싱 후 사용.
- 계산 결과는 `readonly` 로 반환. 호출자가 변형하지 않게 합니다.
- 상태를 나타내는 문자열은 유니온 타입으로 (`'begin' | 'end'`), enum 사용하지 않음.

### 주석
- **"무엇"이 아니라 "왜"를 씁니다.** 특히 세법 근거와 금융 규약 선택 이유는 반드시 주석으로 남깁니다.
```ts
// 국내상장 해외ETF 매매차익은 배당소득으로 과세되므로 250만원 기본공제가 없다.
// 해외상장 ETF(양도소득)와 구분해야 함. 출처: kbthink.com/etf/etf-tax.html (2026-08-08)
```
- 수식을 구현할 때는 수식 자체를 주석에 남깁니다.

---

## 5. 테스트 규칙

### 계산 엔진은 테스트 없이 커밋하지 않는다
`src/calc/` 의 모든 공개 함수는 테스트가 있어야 합니다.

### 골든 테스트 (`tests/golden/`)
손으로 검증하거나 엑셀 `FV`/`PMT` 함수로 대조한 케이스입니다. **이 파일의 기대값은 임의로 수정하지 않습니다.**
테스트가 깨지면 → 기대값을 고치는 게 아니라 **왜 계산이 바뀌었는지 먼저 규명**하고, 정당한 변경이면 근거를 커밋 메시지에 남긴 뒤 갱신합니다.

필수 골든 케이스:
1. 초기 0원, 월 100만원, 연 7%, 10년, 보수 0%, 세금 0% → 엑셀 `FV(7%^(1/12)-1, 120, -1000000, 0, 1)` 와 일치
2. 초기 1억, 추가납입 0, 연 7%, 30년 → `1억 × 1.07^30`
3. 인플레 2%, 30년 → 실질/명목 비율 = `1/1.02^30`
4. 인출률 4%, 자산 10억, 수익률 = 물가상승률 → 25년간 정확히 소진
5. TER 0.5%가 30년 결과를 낮추는 방향이며 크기가 근사식과 일치

### 속성 테스트 (property-based)
아래 불변식은 무작위 입력에 대해 항상 성립해야 합니다.
- 총자산 ≥ 총납입원금 (수익률 ≥ 0 일 때)
- 수익률이 높아지면 최종 자산이 단조 증가
- 보수(TER)가 커지면 최종 자산이 단조 감소
- 물가상승률이 커지면 실질 최종 자산이 단조 감소
- 세금이 있는 계좌의 결과 ≤ 세금 없는 동일 조건 결과
- 인출률이 커지면 자산 고갈 시점이 앞당겨짐

### 실행
```bash
npm run test          # 전체
npm run test:calc     # 계산 엔진만 (개발 중 주로 사용)
npm run typecheck
npm run lint
```

### 빌드 타깃 두 가지

| 명령 | 출력 | 용도 |
|---|---|---|
| `npm run build` | `dist/` (여러 파일) | 정적 호스팅용. CSP·코드 스플리팅·번들 예산 검사 포함. **`file://` 로 열리지 않습니다** |
| `npm run build:local` | `dist-local/index.html` (한 파일) | 서버 없이 더블클릭으로 확인용. JS·CSS·워커 전부 인라인 |

`dist/index.html` 을 파일로 직접 열면 흰 화면이 나옵니다 — 절대경로·module CORS·CSP `'self'` 가 동시에 막습니다. 버그가 아니므로 고치려 들지 말고 `build:local` 이나 `npm run preview` 를 쓰세요.

---

## 6. 세제 데이터 갱신 절차

세법은 매년 바뀝니다. 아래 절차를 지키세요.

1. **기존 룰셋을 수정하지 말고 새 파일을 만듭니다** — `kr-2026.ts` → `kr-2027.ts`
2. 각 항목에 `source`, `asOf`, `status` 를 채웁니다. 출처는 **1차 자료 우선**(기재부·국세청·금융위·국민연금공단), 언론·블로그는 보조.
3. 국회 통과 전 개정안은 반드시 `status: "proposed"`.
4. `src/data/tax/index.ts` 의 룰셋 레지스트리에 등록합니다.
5. 골든 테스트를 실행하고, 세율 변경으로 값이 바뀌는 테스트는 **연도를 고정한 룰셋**을 쓰도록 되어 있는지 확인합니다(골든 테스트는 항상 특정 연도 룰셋을 명시적으로 주입).

**웹 검색이 필요한 경우:** 세율·한도를 기억에서 쓰지 마세요. 반드시 검색해 확인하고 URL과 기준일을 기록하세요.

---

## 7. UI 작업 시 지침

> **UI 작업 전에 `design/08-design-system.md` 를 먼저 읽으세요.**
> 시각 언어(색·타이포·간격·컴포넌트)는 전부 그 문서가 규정합니다. 이 절은 제품 규칙만 다룹니다.

핵심만 옮기면:

- 색·크기는 **토큰만** 씁니다. `src/index.css` 밖에서 hex 를 쓰거나 `slate-500` `text-sm` 같은 Tailwind 기본값을 쓰면 잘못된 코드입니다.
- 컴포넌트는 `src/components/` 의 프리미티브를 씁니다. 화면 파일 안에서 일회성 스타일링을 하지 않습니다.
- 텍스트 글리프(`▼ ✕ ⓘ ⚠ ƒ`) 대신 `src/components/display/Icon.tsx` 의 SVG 아이콘을 씁니다.
- **시리즈 색을 바꾸려면 반드시 dataviz 검증기를 재실행**하고 결과를 `08-design-system.md` §4 에 갱신합니다. 눈으로 판단하지 않습니다.
- 새 화면을 만든 뒤 `08-design-system.md` §8 "AI 티 안티패턴 체크리스트"를 훑습니다.

- **모바일 우선.** 대부분 스마트폰에서 봅니다.
- 숫자 입력은 **슬라이더 + 직접 입력을 함께** 제공하고, 입력 중 한국식 단위 힌트를 보여줍니다(`3,000,000` → `300만원`).
- 결과 대시보드 최상단 순서는 고정입니다: ① 현재 구매력 기준 월 사용 가능액 ② 목표 달성률 ③ 예상 은퇴자산(명목/실질) ④ 총납입원금 / 투자수익.
- 모든 결과 카드에는 **가정 패널로 가는 링크**가 있어야 합니다.
- 차트 색상은 색약 사용자를 고려하고, 색 외에 패턴/레이블로도 구분되게 합니다.
- 로딩이 100ms를 넘는 계산(Monte Carlo)은 진행률을 표시합니다.
- 면책 문구("투자 권유가 아니며 세금 계산은 참고용입니다")는 결과 화면에서 항상 보이게 둡니다.

---

## 8. 하지 말아야 할 것

- ❌ 특정 금융상품·티커를 추천하는 문구 삽입
- ❌ "이 정도면 은퇴 가능합니다" 같은 단정적 조언 (사실과 계산 결과만 제시)
- ❌ 실현 불가능한 기본값 (기대수익률 12% 기본값 등). 기본값은 보수적으로.
- ❌ 세금·건보료를 "대략" 반영하고 정확한 것처럼 표시 (근사임을 반드시 명시)
- ❌ 계산 엔진에 UI 관심사 침투 (포맷된 문자열 반환, 색상 결정 등)
- ❌ `plan/계산기 초기 설계.txt` 원본 수정 (사용자 원안 보존)
- ❌ 골든 테스트 기대값을 근거 없이 갱신

---

## 9. 작업 흐름

1. 구현 전 해당 `design/` 문서를 읽습니다. 설계와 다르게 구현해야 한다면 **먼저 설계 문서를 수정**하고 그 이유를 남깁니다.
2. `src/calc/` → 테스트 → UI 순서로 작업합니다. UI를 먼저 만들지 마세요.
3. 커밋 전: `npm run typecheck && npm run lint && npm run test`
4. 세법·제도 관련 변경은 커밋 메시지에 출처 URL과 기준일을 포함합니다.

---

## 10. 용어집

| 용어 | 정의 |
|---|---|
| 명목(nominal) | 미래 시점의 액면 금액 |
| 실질(real) | 오늘 구매력으로 환산한 금액 |
| TER | ETF 연간 총보수 (Total Expense Ratio) |
| SWR | 안전인출률 (Safe Withdrawal Rate) |
| SORR | 서열수익률 위험 (Sequence of Returns Risk) — 은퇴 초기 하락장이 결과를 크게 악화시키는 현상 |
| 축적기(accumulation) | 은퇴 전, 자산을 쌓는 기간 |
| 인출기(decumulation) | 은퇴 후, 자산을 쓰는 기간 |
| 브리지 기간 | 은퇴 시점 ~ 연금 수령 개시 사이, 투자자산만으로 버티는 구간 |
| 오버플로 배분 | 계좌 납입한도 초과분을 다음 우선순위 계좌로 넘기는 로직 |
| Gross-up | 세후 필요액에서 세전 인출액을 역산하는 계산 |
| VPW | 매년 잔여자산·잔여기간으로 인출액을 재산정하는 방식 (Variable Percentage Withdrawal) |
